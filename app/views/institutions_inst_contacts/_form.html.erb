<%= form_with(model: institutions_inst_contact, local: true) do |form| %>
  <% if institutions_inst_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(institutions_inst_contact.errors.count, "error") %> prohibited this institutions_inst_contact from being saved:</h2>

      <ul>
      <% institutions_inst_contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
