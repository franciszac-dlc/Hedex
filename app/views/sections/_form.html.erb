<%= form_with(model: section, local: true) do |form| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :section_name %>
  </div>

  <div class="field">
    <%= form.label :head_count %>
    <%= form.number_field :head_count, id: :section_head_count %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :section_year %>
  </div>

  <div class="field">
    <%= form.label :grade_level %>
    <%= form.number_field :grade_level, id: :section_grade_level %>
  </div>

  <div class="field">
    <%= form.label :institution_id %>
    <%= form.text_field :institution_id, id: :section_institution_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
